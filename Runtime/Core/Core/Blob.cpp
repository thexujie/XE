#include "PCH.h"
#include "Blob.h"
//#include "IO/File.h"
//#include "IO/FileStream.h"

namespace XE
{
	FFileBlob::FFileBlob(FStringView Path)
	{
		//FFileStream File(Path, EFileMode::In);
		//if (File.IsValid())
		//{
		//	Data.Resize(File.GetSize(), File.GetSize(), EInitializeMode::None);
		//	File.Read(Data.GetData(), Data.GetSize());
		//	File.Close();
		//}
	}
}
